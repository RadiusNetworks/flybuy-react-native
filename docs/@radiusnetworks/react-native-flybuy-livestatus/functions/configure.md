[**Documentation**](../../../README.md)

***

# Function: configure()

> **configure**(`icon`, `statusTintColor?`, `statusTintDarkModeColor?`): `Promise`\<`string`\>

Defined in: [index.tsx:34](https://github.com/RadiusNetworks/flybuy-react-native/blob/main/mono/packages/livestatus/src/index.tsx#L34)

Configure LiveStatus with options.

## Parameters

### icon

`string`

The icon name to use.

### statusTintColor?

`string`

The status tint color (hex string, e.g. '#A162F7').

### statusTintDarkModeColor?

`string`

The status tint color for dark mode (hex string, e.g. '#00E5C8').

## Returns

`Promise`\<`string`\>

Promise<string>
